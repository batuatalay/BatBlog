@layout('main')

@section('content')
    <h1>{{subTitle}}</h1>
    <ul class="list-group">
        @each(item in postData)
        <li class="list-group-item mb-3 shadow">{{item.title}}</li>
        @endeach
    </ul>
@endsection